#!/usr/bin/env bash

if [ $(uname -s) = "Darwin" ]; then
    alias ls='ls --color=auto'
    alias ll='ls -al'
fi

for x in bin/ data/; do
    if [ ! -d $x ]; then
        mkdir $x
    fi
done

export WD=$(pwd)
